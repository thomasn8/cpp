### ClassGen ### 

# Enregistrer le script dans un directory mentionné dans le $PATH :

	$ echo $PATH
	/Users/thomas/.brew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin

	$ cp ClassName.sh /usr/local/bin

# Créer une alias pour "ClassList.sh" dans .zshrc

	vim .zshrc

	insert : 
	alias class="ClassList.sh"

# Utiliser cette alias (class) pour utiliser ce script depuis n'importe où
et générer des classes dans le dossier courant
